name: Beta

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      ref:
        description: "Ref to build (default: current)"
        required: false
        default: ""

permissions:
  contents: read

jobs:
  build:
    runs-on: macos-26
    env:
      APP_BUNDLE_ID: ${{ vars.APP_BUNDLE_ID || 'com.iSoldLeo.PastScreen-CN' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref || github.ref }}

      - name: Build arm64
        shell: bash
        run: |
          EXTRA_ARGS=()
          if [[ -n "${APP_BUNDLE_ID}" ]]; then
            EXTRA_ARGS+=("PRODUCT_BUNDLE_IDENTIFIER=${APP_BUNDLE_ID}")
          fi

          xcodebuild \
            -project PastScreen-CN.xcodeproj \
            -scheme PastScreen-CN \
            -configuration Release \
            -derivedDataPath build \
            ARCHS=arm64 \
            ONLY_ACTIVE_ARCH=NO \
            CONFIGURATION_BUILD_DIR=build/Release-arm64 \
            CODE_SIGNING_ALLOWED=NO \
            "${EXTRA_ARGS[@]}"

      - name: Package zip
        shell: bash
        run: |
          APP_PATH="build/Release-arm64/PastScreen-CN.app"
          VERSION="${GITHUB_SHA::7}"
          ZIP_NAME="PastScreen-CN-beta-${VERSION}-macOS-arm64.zip"
          ditto -c -k --sequesterRsrc --keepParent "$APP_PATH" "$ZIP_NAME"
          shasum -a 256 "$ZIP_NAME" > "${ZIP_NAME}.sha256"
          echo "ZIP_NAME=${ZIP_NAME}" >> $GITHUB_ENV

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: PastScreen-CN-beta-arm64
          path: |
            PastScreen-CN-beta-*-macOS-arm64.zip
            PastScreen-CN-beta-*-macOS-arm64.zip.sha256
          if-no-files-found: error
