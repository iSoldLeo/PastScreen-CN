# PastScreen 产品观审视：对照张小龙 139 页产品观

> 基于《微信背后的产品观》（张小龙）+ PastScreen 半年实际使用反馈（开发者本人，~1500 张截图）。
> 由 `zhang-xiaolong-applied.md` 与 `zhang-xiaolong-deep-analysis.md` 合并整理。

---

## 目录

- [一、产品 DNA 与一句话定位](#一产品-dna-与一句话定位)
- [二、对照张小龙原则的成绩单](#二对照张小龙原则的成绩单)
  - [2.1 已暗合的原则（做对了的）](#21-已暗合的原则做对了的)
  - [2.2 已违反的原则（该改的）](#22-已违反的原则该改的)
  - [2.3 可深度借鉴的原则（启发的）](#23-可深度借鉴的原则启发的)
- [三、PastScreen 的减法清单](#三pastscreen-的减法清单)
  - [3.1 P0：立即砍](#31-p0立即砍)
  - [3.2 P1：设置 Tab 重组（5 → 3）](#32-p1设置-tab-重组5--3)
  - [3.3 P2：交互重做](#33-p2交互重做)
  - [3.4 P3：如果要加功能](#34-p3如果要加功能)
- [四、改进的具体执行方案](#四改进的具体执行方案)
  - [4.1 Onboarding 6 步 → 2 步](#41-onboarding-6-步--2-步)
  - [4.2 编辑设置页所见即所得](#42-编辑设置页所见即所得)
  - [4.3 文案审查清单](#43-文案审查清单)
- [五、DNA 守护准则](#五dna-守护准则)
- [六、附：统一入口为何被否决](#六附统一入口为何被否决)
- [七、待决问题](#七待决问题)

---

## 一、产品 DNA 与一句话定位

> **「截图的最短路径。」**
>
> PastScreen 是一把快刀，不是一个工具箱。它的价值是让「截图→剪贴板」这个动作快到用户感知不到延迟。其他一切都是锦上添花，不能让刀变钝。

### DNA 的具体体现

| 维度 | 表现 |
|------|------|
| **速度** | 热键响应 < 50ms，截图到剪贴板 < 200ms |
| **路径** | 热键→框选/点窗→完成，零中间步骤 |
| **存在感** | 菜单栏图标，不占 Dock，不弹窗，不打扰 |
| **可靠性** | 每次都成功，不崩溃，不丢图 |
| **克制** | 不做不该做的事，不让产品变重 |

### 不属于 DNA 的东西（应该砍掉或降级）

- 素材管理（标签 / 备注 / 搜索 / 语义）
- 应用规则（按 App 切换剪贴板格式）
- 复杂配置（让用户做选择）
- 使用指南弹窗（如果需要指南，说明产品不够直觉）

---

## 二、对照张小龙原则的成绩单

### 2.1 已暗合的原则（做对了的）

| # | 原典原则（页码） | PastScreen 表现 |
|---|------------------|-----------------|
| 1 | 人是懒惰的（P7） | ✅ 热键一按图进剪贴板，零步骤；不需要打开任何窗口完成核心任务 |
| 2 | 人没有耐心（P9） | ✅ 截图响应极快，用户感知不到延迟 ⚠️ 但 Onboarding 6 步太长，违反「一分钟爱上你」 |
| 3 | 满足自己需求优于满足用户需求（P26） | ✅ 自己用了半年、1500 张截图，是最强的需求验证 |
| 4 | 只抓主场景，不做全功能（P33） | ✅ 核心场景做到极致 ❌ 素材库、应用规则、语义搜索偏离了这个原则 |
| 5 | 操作的响应速度永远是第一体验（P65） | ✅ 这是 PastScreen 的生命线，一直在优化 |
| 6 | 极简方能不被超越（P63） | ✅ 核心截图流程极简——这就是 macOS 自带截图无法替代它的原因（系统截图多了「选择保存位置/点击通知」一步） |
| 7 | 尊重用户（P73） | ✅ 离线运行，不联网、不上传、不追踪；权限请求有明确说明用途 |
| 8 | 做一劳永逸的事情（P61） | ✅ 截图功能一次做好，不需要运营、不需要内容、不需要社区 |

**附：5 条暗合且不再展开的小条目**

- 多热键分工明确（⌥⌘S 选区进剪贴板 / ⌥⌘⇧S 标注 / ⌥⌘⇧O OCR）类似 Ctrl+C / Ctrl+V 的逻辑，零决策成本
- 截图直接进剪贴板，无中间弹窗——产品的灵魂
- 菜单栏应用不占 Dock——存在感极低
- 离线运行零联网——隐私即尊重
- 成功反馈一闪即逝，失败时才说话——不打扰

### 2.2 已违反的原则（该改的）

| # | 原典原则（页码） | PastScreen 违反点 |
|---|------------------|------------------|
| 1 | 大部分新功能是可以砍掉的（P19） | 素材库、应用规则、11 种语言 + 文言文 + 南极语、使用指南弹窗、调试模式开关（暴露给用户） |
| 2 | "管理"是不自然的（P92） | 素材库的标签、备注、置顶、分组都是「管理」行为。截图不是照片，用户不会回来管理它们——开发者半年只打开 2-3 次就是证明 |
| 3 | 不让用户选择（P95） | 边框宽度/圆角/颜色（3 个独立数值）、工具栏 8 个工具的启用/排序、快捷轮盘工具选择、OCR 12 种语言 checkbox、清理策略 3 个数值 |
| 4 | 非重要特性就放到设置里，放到设置里不如不做（P69） | 直接判了死刑：素材库所有配置项、应用规则、调试模式、存预览图开关 |
| 5 | 需要文字解释的功能不是好体验，少用 tip 像膏药（P133） | 使用指南弹窗就是一大块「膏药」。需要弹窗解释怎么用，说明产品本身不够直觉 |
| 6 | 不从同类产品里找需求（P20） | 素材库的标签/备注/搜索语法是从 Eagle/Pixave 等素材管理器借来的需求。但截图工具用户不是设计师，不需要管理素材 |
| 7 | 如果解决方案非常复杂，一定是问题错了（P70） | 应用规则：为了解决「终端需要路径而非图片」，做了按应用匹配的规则系统。但问题本身已不存在（AI 工具都能接受图片粘贴） |

### 2.3 可深度借鉴的原则（启发的）

> 这一节不是「违反了」，而是「可以做得更好」。

#### ① 让功能存在于无形之中（P57）

> 让新版看不出有变化。只有新手才将所有新功能罗列在显眼的地方。

- 高级截图的标注工具不需要在设置里配置，应在编辑器里「自然存在」
- OCR 的语言配置可以去掉——默认自动检测就够了
- 但 OCR 独立热键是对的（高频确定性操作需要直达路径，详见 §6 附录）

#### ② 做三秒就会用的特性（P91）

- 截图核心流程已经做到三秒会用
- 但**设置页面没有做到**：边框配置需反复调试，快捷轮盘需理解概念
- 改进：边框用预设 + 实时预览，轮盘所见即所得直接操作

#### ③ 产品是演化出来的而非规划出来的（P45）+ 有 DNA 的产品才会演化（P46）

- 不要提前规划「下一步做什么功能」
- 用半年，看自己缺什么，那就是下一个版本的需求
- 半年没用素材库 → 它不是需求
- 半年截了 1500 张图 → 速度和稳定性才是需求
- 所有演化都应围绕 DNA：更快、更准、更稳，不是更多功能

#### ④ 宁愿损失功能也不损失体验（P60）

- 如果素材库的后台索引 / OCR / 语义搜索哪怕让截图慢了 1ms，就应该砍
- 如果边框渲染让截图到剪贴板时间变长，就应该默认关闭
- **功能可以没有，体验不能打折。**

#### ⑤ 面向两年后的场景来设计（P64）

**2028 年截图场景预测：**
- AI 无处不在，截图的主要用途可能是「给 AI 看」
- 所有应用都能接受图片粘贴（应用规则彻底过时）
- 屏幕分辨率更高，截图文件更大，速度和压缩更重要
- 可能需要的：智能裁剪（自动识别有意义的区域）、即时 OCR 到剪贴板

#### ⑥ 文字反映气质（P116）+ 避免使用的词语（P117）

> 产品中的文字是你和用户在说话。清晰易懂。不讨好用户。把用户当朋友。
>
> 不用「成功」（「已保存成功」，有已保存失败？）

**PastScreen 文案审查**（详见 §4.3）：
- ✅ 「截图后复制：图片」——清晰
- ⚠️ 「自动 OCR 以增强搜索（可能耗时/耗电）」——括号里像在推卸责任
- ⚠️ 「语义增强（实验，仅离线；不可用时自动降级）」——技术术语，用户不懂
- ❌ 「存预览图（支持未落盘时再次复制）」——括号里的解释说明功能命名本身不清晰
- ❌ 「截图成功」/「授权成功」——应改为「已复制到剪贴板」/「已就绪」

#### ⑦ 每个界面只有一个主题（P130）+ 只有一种式样（P129）

> 一个应用中，尽可能只有一种字体，一类控件，一种式样。

- 通用页：选项 + 帮助 + 隐私 = 三个主题混在一起
- 存储页：存储位置 + 素材库配置 = 两个主题（砍掉素材库后解决）
- 编辑页：工具栏 + 快捷轮盘 + OCR 语言 = 三个主题
- 使用指南弹窗的视觉风格和设置窗口完全不同——「精神分裂」，要么统一要么砍

#### ⑧ 如果一个特性不能让人兴奋，不如不做（P66）

> 在单点获得口碑，而不是一系列可有可无的特性。

**PastScreen 的兴奋单点**：「快。按下去就有了。比系统截图快，比 CleanShot X 轻。」

这就是口碑点。所有精力应投入到让这个点更极致，而不是分散到素材库、应用规则、语义搜索上。

---

## 三、PastScreen 的减法清单

### 3.1 P0：立即砍

| 砍掉项 | 砍掉理由 | 关联原则 |
|--------|----------|---------|
| **素材库** 或降级为「最近截图列表」 | 半年只打开 2-3 次；Finder 已覆盖回溯需求；标签/备注是伪需求；搜索语法学习成本 > 收益 | §2.2 #1 #2 #6 |
| **应用规则功能** + 对应设置 Tab | 前提假设过时——Codex CLI / Claude Code / VS Code Copilot 都支持图片粘贴；需要用户「提前配置」违反「人是懒的」 | §2.2 #7 |
| **使用指南弹窗** | 排版粗糙、信息层级混乱、视觉风格与设置窗口割裂；需要文字解释的功能不是好体验 | §2.2 #5；§2.3 ⑦ |
| 调试模式开关（暴露给用户） | 不应让用户看到 | §2.2 #4 |
| 存预览图开关 | 命名都解释不清自己，说明该砍 | §2.2 #4 |

**素材库的深层诊断**：

它不是「做得不好」，而是**它解决的问题本身不存在**：
1. 截图的生命周期极短——绝大多数用途是「截完→粘贴→结束」，不需要回溯
2. 需要回溯时 Finder + 时间排序 + Quick Look 已够用
3. 标签/备注是「图片管理器」思维，不是「截图工具」思维
4. 它的存在更多是开发者的技术成就感（SQLite + 语义搜索 + 复杂过滤），而非用户真实需求

**张小龙会怎么做**：砍掉它，或者把它降级为「最近截图列表」——就是菜单栏里已有的「最近 10 条」。

### 3.2 P1：设置 Tab 重组（5 → 3）

**当前结构**：通用 / 截图 / 编辑 / 应用 / 存储

**精简动作**：

| Tab | 处理 | 说明 |
|-----|------|------|
| 应用 | ❌ 移除 | 应用规则功能砍掉后该 Tab 失去存在意义 |
| 存储 | 🔁 合并 | 只剩「保存到磁盘开关 + 路径」，合并入通用或截图 |
| 通用 | ✅ 保留 | 吸收存储后内容密度更合理 |
| 截图 | ✅ 保留 | 核心配置区 |
| 编辑 | ✅ 保留 | 但内部交互重做（详见 §4.2） |

**精简后存储页只剩**：
- 「保存到磁盘」开关
- 保存位置路径 + 更改/打开文件夹按钮

**最终设置 Tab 结构**：通用、截图、编辑（3 个）。

### 3.3 P2：交互重做

#### 编辑设置页（功能有用，交互不好用）

**① 工具栏（启用 + 排序）**
- 问题：拖拽手柄（≡）视觉不明显，列表看起来像静态展示
- 问题：8 个工具平铺，但多数人不会关任何工具，也不会排序
- 改进：默认全部启用且不显示排序 UI，只在用户主动进入「自定义」模式才展开

**② 快捷轮盘配置**
- 问题：配置方式（下拉选工具 + 删除按钮 + 拖拽排序）步骤多
- 问题：下方轮盘预览与上方配置列表视觉割裂
- 改进：直接在轮盘预览上点击槽位选择工具，所见即所得

**③ OCR 语言选择**
- 问题：十几种语言 checkbox，多数人只勾 1-2 种
- 问题：底部提示「不勾选则自动检测」——那为什么不默认就自动检测？
- 改进：默认「自动检测」，给有需要的人一个「指定语言」展开入口

#### 通用页面 + 使用指南视觉重构

- **通用页**：选项太少占了一整个 Tab 显得空洞——存储合并进来后改善
- **使用指南**：要么砍掉（用户真需要帮助会看 README），要么重做成 macOS 原生 Tips 卡片式（每个功能一张卡片，配图 + 一句话）。参考 Raycast onboarding tips / Arc 功能引导

#### 边框配置预设化
- 当前：3 个独立数值（宽度 / 圆角 / 颜色）
- 改进：3 个预设（无 / 轻 / 重）+ 自定义入口

#### 截图成功反馈再轻一点
- Dynamic Island 闪一下就够，不需要任何可交互元素

#### 窗口截图命中率
- 悬停高亮要更准确，减少「没选中想要的窗口」

#### 语义搜索
- 标记为实验并默认关闭——增加资源消耗但几乎无人使用

### 3.4 P3：如果要加功能

**不加**——除非有明确的、高频的、现有方案无法满足的用户场景。

> 张小龙的检验标准：**「如果这个功能不做，用户会不会来骂我？」**
> 如果不会，就不做。

---

## 四、改进的具体执行方案

### 4.1 Onboarding 6 步 → 2 步

**当前 6 步**包含权限授予 + 应用规则配置 + 各类选项介绍。

**简化为 2 步**：
1. 权限授予（屏幕录制权限——这是必须的）
2. 完成（其他能力渐进式披露，用户用到时再讲）

应用规则砍掉后，Onboarding 自动减少 1 步；使用指南弹窗砍掉后，再减少 1-2 步。

### 4.2 编辑设置页所见即所得

| 区块 | 当前交互 | 改造后 |
|------|---------|--------|
| 工具栏 | 8 个工具 checkbox + 拖拽手柄 | 默认全开，「自定义」按钮折叠，进入后才显示排序 UI |
| 快捷轮盘 | 上方列表配置 + 下方预览 | 直接在轮盘预览上点击槽位选择工具 |
| OCR 语言 | 12 种 checkbox 平铺 | 默认「自动检测」单选，「指定语言」展开入口 |
| 边框 | 宽度/圆角/颜色三个数值 | 3 个预设（无/轻/重）+ 自定义入口 |

### 4.3 文案审查清单

| 当前文案 | 问题 | 改造方向 |
|---------|------|---------|
| 「截图成功」 | 有「截图失败」吗？冗余 | 「已复制到剪贴板」 |
| 「授权成功」 | 同上 | 「已就绪」 |
| 「自动 OCR 以增强搜索（可能耗时/耗电）」 | 括号里像推卸责任 | 直接说「自动 OCR」，副作用不强调或砍功能 |
| 「语义增强（实验，仅离线；不可用时自动降级）」 | 用户不懂「语义增强」 | 改名为用户能理解的词，或者标记为实验默认关闭 |
| 「存预览图（支持未落盘时再次复制）」 | 括号解释 = 命名不清 | 重命名或直接砍 |

**审查原则**：
- 不用「成功」类冗余词
- 不在括号里写技术术语 / 副作用警告
- 命名要让用户不需要解释就能懂；要解释就改名

---

## 五、DNA 守护准则

### 立即做（减法，1-2 周内）

1. 移除应用规则功能 + 对应设置 Tab
2. 移除素材库或降级为「最近截图列表」
3. 移除使用指南弹窗
4. Onboarding 6 步 → 2 步
5. 设置 Tab 5 → 3

### 后续做（打磨，1-2 个月）

6. 编辑设置页交互重设计（所见即所得）
7. 通用页视觉重构
8. 边框配置预设化
9. OCR 语言默认自动检测，去掉 checkbox 列表
10. 文案审查（按 §4.3 清单）

### 持续做（DNA 守护，每个版本）

11. 截图管线性能持续优化（capture-pipeline-perf）
12. 每个版本发布前问自己：**「这个改动让截图变快了还是变慢了？」**
13. 每个新功能提案前问自己：**「如果不做这个，用户会来骂我吗？」**
14. **「面向两年后的场景」**：所有设计要考虑 2028 年 AI 普及后的截图场景

### P0 守护红线

- 截图速度、剪贴板写入速度、热键响应速度——这些是生命线
- 任何新功能如果让这三个指标变慢 1ms，都不值得
- **宁愿损失功能也不损失体验。**

---

## 六、附：统一入口为何被否决

曾考虑过「一个热键触发，截完后再选择标注/OCR/复制」的统一入口模式。

**否决理由**：

1. **违背「用完即走」**——截完还要多一步选择，阻挡最快路径
2. **熟练用户的肌肉记忆是分意图的**：
   - ⌥⌘S = 选区进剪贴板
   - ⌥⌘⇧S = 标注
   - ⌥⌘⇧O = OCR
   - 三个独立意图，三个独立入口，零认知切换
3. **键盘快捷键的本质就是「用不同的按键组合表达不同的意图」**——Ctrl+C 和 Ctrl+V 没有人会觉得应该合并成一个键再弹菜单选复制还是粘贴
4. **统一入口适合低频、探索性操作；高频、确定性操作必须有直达路径**

**结论**：多热键分工是正确设计，保持不变。

---

## 七、待决问题

### 关于素材库的最终决策

1. **是否有其他用户在活跃使用素材库？**
   - 如果有，保留但简化；如果没有，考虑移除
2. **「最近 10 条」菜单是否已覆盖素材库 90% 的使用场景？**
   - 大概率是
3. **如果保留，是否应该降级为「历史记录」？**
   - 去掉标签/备注/语义搜索，只保留时间线 + 缩略图 + 复制

### 最后一页的启示（P138）

> 「我所说的，都是错的。」——张小龙

所有原则都有例外，产品没有公式。但有一条不会错：

**开发者自己用了半年、截了 1500 张图、几乎没打开过素材库——这就是最真实的产品反馈。相信使用直觉，而非理性分析出来的「应该有」。**

---

*文档整理日期：2026-05-18*
*合并来源：`zhang-xiaolong-applied.md` + `zhang-xiaolong-deep-analysis.md`*
*对照原典：《微信背后的产品观》张小龙 139 页手抄版*
